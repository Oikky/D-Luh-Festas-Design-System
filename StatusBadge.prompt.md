One-line: the loading ring — block mode for a whole list, inline mode inside a busy button.

```jsx
<Spinner label="Carregando pedidos…" />
<Spinner size={15} inline />
```

Terracotta head on a sand-coloured track, 800ms linear. Never use it for anything under ~300ms.
