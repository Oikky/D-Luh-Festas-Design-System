One-line: the "nothing here" block for an empty tab, an empty search, or a cleared queue.

```jsx
<EmptyState icon="party-popper" title="Nenhum pedido pendente"
  description="Tudo confirmado por aqui. Novos pedidos aparecem automaticamente." />
```

Copy is calm and factual in pt-BR; never scold the user. Add an action only when there is one obvious next step.
