One-line: the signed-in person in the top bar — 38px rounded-square avatar, name, role.

```jsx
<UserChip name="Luciana" role="Gerente" />
<UserChip name="Cozinha" compact />
```

Rounded square, never a circle — circles are reserved for status dots. Falls back to the first letter on terracotta tint.
