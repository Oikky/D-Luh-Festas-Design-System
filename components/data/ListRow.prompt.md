One-line: one line of a feed — a payment, a queued order, a recent customer.

```jsx
<ListRow icon="cake-slice" title="Bolo Vulcão 2kg" subtitle="Maria Helena · 14:30" value="R$ 240,00" tone="in" />
```

Rows sit in a plain flex column with `gap: 6px` — no table borders. Keep the title to one line; it truncates.
