One-line: a small trend line for a StatCard or a chart card header — no axes, no labels, no tooltip.

```jsx
<Sparkline data={[12, 18, 15, 24, 22, 31, 28]} />
```

Terracotta stroke with a 22%→0 gradient underneath. For anything that needs axes, use a real chart card in the UI kit.
