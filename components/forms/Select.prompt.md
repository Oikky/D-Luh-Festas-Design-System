One-line: native `<select>` in D'Luh clothing; `accent` is the status changer sitting on an order card.

```jsx
<Select options={["Pix", "Cartão", "Dinheiro"]} />
<Select accent options={Object.keys(STATUS)} defaultValue="Pago — Em produção" />
```

Stays a native select on purpose — it is the only control that works well on the kitchen tablet.
