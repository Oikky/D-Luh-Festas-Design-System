One-line: a small pill for counts, flags and attributes — not for order status (use `StatusBadge`).

```jsx
<Badge tone="warn">3 alterações não enviadas</Badge>
<Badge tone="accent" icon="building-2">Empresa</Badge>
```

Live examples from the admin: "🏢 Empresa", "N alterações não enviadas", "Pedido manual".
